{
  "name": "rainknights",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "art:generate": "node --env-file=.env scripts/generate-art.mjs",
    "capture": "playwright test"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
